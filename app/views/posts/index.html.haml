%h1 Listing posts

%table
  %thead
    %tr
      %th Student
      %th Reviewed
      %th Title
      %th Link
      %th Coachability
      %th
      %th
      %th

  %tbody
    - @posts.each do |post|
      %tr
        %td= post.student
        %td= post.reviewed
        %td= post.title
        %td= post.link
        %td= post.coachability
        %td= link_to 'Show', post
        %td= link_to 'Edit', edit_post_path(post)
        %td= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Post', new_post_path
