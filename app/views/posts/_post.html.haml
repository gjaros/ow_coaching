%button.btn.btn-primary.btn-block.font-weight-bold(data-target='#post-#{post.id}' data-toggle='modal' type='button')= post.title

.modal.fade(id='post-#{post.id}' aria-hidden='true' aria-labelledby='exampleModalLabel' role='dialog' tabindex='-1')
  .modal-dialog(role='document')
    .modal-content
      .modal-header
        %h5#exampleModalLabel.modal-title= post.title
        %button.close(aria-label='Close' data-dismiss='modal' type='button')
          %span(aria-hidden='true') ×
      .modal-body ...
      .modal-footer
        %button.btn.btn-secondary(data-dismiss='modal' type='button') Close
